// ReCharts styling configuration
import colors from './../../colors';

export default {
    grid: {
        stroke: colors['900'],
        strokeWidth: 1,
        strokeDasharray: '3px'
    },
    axis: {
        stroke: colors['primary'],
        strokeWidth: 2,
        style: {
            fontSize: '12px'
        }
    }
};