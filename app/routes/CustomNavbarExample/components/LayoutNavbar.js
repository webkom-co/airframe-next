import React from 'react';
import {
    Navbar
} from './../../../components';

export const LayoutNavbar = () => (
    <React.Fragment>
        <Navbar>
            First Sidebar
        </Navbar>

        <Navbar>
            Sidebar Sidebar
        </Navbar>
    </React.Fragment>
);
